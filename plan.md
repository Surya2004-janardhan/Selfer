THis is a plan for building a self-improving AI agent.

named selfer 

selfer is cli based application that works both locally powered ollama model also with fronteir cloud models

selfer is self learning self improvement agent that can do following things:

all the tasks functiins that src-refernce can do 

i want u to build this project in a way that it uses exactly src-refernce based code in the root folder 

src-refernce is not in our main project but is there to give us full idea full code to make this project succes make selfer a cli based great agent with linux based green and blue theme color nd nice animations use existing thing src-reference one for best code

strict note : u shd not use same function names of src-reference used we shd change little functions tools names agent names file names strucutre in our root repo even vairable names must be changed little so that there shd not be name of us saying something they copied 


- donot edit src-refernce/ anywhere